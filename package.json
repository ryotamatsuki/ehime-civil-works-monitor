{
  "name": "ehime-civil-works-monitor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "lint": "eslint .",
    "test": "vitest run",
    "build": "vite build && node scripts/generate-detail-pages.mjs",
    "validate": "python3 scripts/validate_data.py && python3 scripts/validate_enrichment.py && python3 scripts/validate_annual_budget.py && python3 scripts/validate_phase26.py && python3 scripts/validate_phase27.py",
    "report:inventory": "python3 scripts/report_inventory.py",
    "report:budget": "python3 scripts/report_annual_budget.py",
    "check": "npm run lint && npm test && npm run validate && npm run report:inventory && npm run report:budget && npm run build"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "@eslint/js": "^9.34.0",
    "@types/leaflet": "^1.9.20",
    "eslint": "^9.34.0",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.41.0",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
